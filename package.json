{
  "scripts": {
    "build":"cd frontend && npm install && npm run build && cd.. && cd DemoPage && npm install &&npm run build && cd ../backend && npm install",
    "dev": "cd backend && npm run dev"

  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "emoji-picker-react": "^4.12.2",
    "lucide-react": "^0.484.0"
  }
}
